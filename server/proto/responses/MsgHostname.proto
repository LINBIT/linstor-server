syntax = "proto2";

package com.linbit.linstor.proto.responses;

// Message returning the hostname
// Requested by: Header-only call, ApiConsts.API_HOSTNAME
message MsgHostname
{
    optional string hostname = 1;
}
