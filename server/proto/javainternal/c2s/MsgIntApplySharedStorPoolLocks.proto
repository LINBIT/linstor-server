syntax = "proto3";

package com.linbit.linstor.proto.javainternal.c2s;

// Internal message that the controller granted the given requested shared stor pool locks
message MsgIntApplySharedStorPoolLocks
{
    repeated string shared_stor_pool_locks = 1;
}
