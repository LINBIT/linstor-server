syntax = "proto2";

package com.linbit.linstor.proto.common;
option go_package = "common";

import "linstor/proto/common/ProviderType.proto";

// A wrapper for the  ProviderType enum. This message is only used to wrap a proto2 enum to make it usable from proto3 messages.
message ProviderTypeWrapper
{
    required common.ProviderType type = 1;
}
