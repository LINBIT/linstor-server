syntax = "proto2";

package com.linbit.linstor.proto.responses;
option go_package = "responses";

import "linstor/proto/common/LinStorMapEntry.proto";

// List of controller config properties
// Requested by: Header-only call, ApiConsts.API_LST_CTRL_PROPS
message MsgLstCtrlCfgProps
{
    repeated common.LinStorMapEntry props = 1;
}
