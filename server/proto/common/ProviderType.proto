syntax = "proto3";

package com.linbit.linstor.proto.common;

enum ProviderType
{
    UNKNOWN_PROVIDER = 0;

    DISKLESS = 1;

    LVM = 2;
    LVM_THIN = 3;
    ZFS = 4;
    ZFS_THIN = 5;
    FILE = 8;
    FILE_THIN = 9;
    SPDK = 10;
    OPENFLEX_TARGET = 11;
    EXOS = 12;
    REMOTE_SPDK = 13;
}
