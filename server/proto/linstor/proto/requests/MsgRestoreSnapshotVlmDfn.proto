syntax = "proto2";

package com.linbit.linstor.proto.requests;
option go_package = "requests";

// Create volume definitions from snapshot
// ApiConsts.API_RESTORE_VLM_DFN
message MsgRestoreSnapshotVlmDfn
{
    // Source resource name
    required string from_resource_name = 1;

    // Snapshot name
    required string from_snapshot_name = 2;

    // Target resource name
    required string to_resource_name = 3;
}
